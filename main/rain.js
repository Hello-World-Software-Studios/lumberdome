export class RainDrop {
    constructor (size, dropX, dropY, fallSpeed) {
        this.size = size;
        this.dropX = dropX;
        this.dropY = dropY;
        this.fallSpeed = fallSpeed;
    }
    
}